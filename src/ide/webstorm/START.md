# 🚀 Как запустить WebStorm с плагином

## Три простых шага:

### 1. Настройте Java (один раз)

Откройте терминал и выполните:

```bash
export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"
export JAVA_HOME="/opt/homebrew/opt/openjdk@17"
```

**Чтобы не делать это каждый раз**, добавьте в `~/.zshrc`:
```bash
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
echo 'export JAVA_HOME="/opt/homebrew/opt/openjdk@17"' >> ~/.zshrc
source ~/.zshrc
```

### 2. Соберите TypeScript код

```bash
cd /Users/maksim.zhuikov/work/point-i18n
pnpm run build:webstorm
```

### 3. Запустите плагин

```bash
cd src/ide/webstorm
./gradlew runIde
```

## ✅ Готово!

Через 30-60 секунд откроется WebStorm с вашим плагином.

---

## Что дальше?

В запущенном WebStorm:
- Откройте любой файл с ключами локализации (например, `$t('some.key')`)
- Наведите курсор на ключ - увидите переводы
- Меню `Tools > Point I18n` - команды плагина

## Проблемы?

Если не работает, проверьте:
```bash
java -version  # Должна быть версия 17
cd src/ide/webstorm && ./gradlew --version  # Должен работать
```

