# 🚀 Запуск плагина WebStorm

## Вариант 1: Через IntelliJ IDEA (рекомендуется)

1. **Откройте проект в IntelliJ IDEA:**
   - File > Open > выберите папку `/Users/maksim.zhuikov/work/point-i18n`

2. **Дождитесь синхронизации Gradle:**
   - IntelliJ IDEA автоматически определит Gradle проект в `src/ide/webstorm/`
   - Дождитесь завершения синхронизации (может занять несколько минут при первом запуске)

3. **Запустите плагин:**
   - Откройте панель Gradle: `View > Tool Windows > Gradle`
   - Найдите: `point-i18n > src/ide/webstorm > Tasks > intellij > runIde`
   - Двойной клик на `runIde` для запуска
   - Или правой кнопкой > `Debug 'runIde'` для отладки

## Вариант 2: Установить Gradle и запустить через командную строку

```bash
# Установите Gradle (если еще не установлен)
# macOS:
brew install gradle

# Или скачайте с https://gradle.org/install/

# Затем создайте wrapper и запустите:
cd src/ide/webstorm
gradle wrapper
./gradlew runIde
```

## Вариант 3: Использовать скрипт (после установки Gradle)

```bash
cd src/ide/webstorm
./run-plugin.sh          # Обычный запуск
./run-plugin.sh --debug  # С отладкой
```

## Что произойдет

1. TypeScript код будет скомпилирован (уже готов в `out/webstorm/`)
2. Gradle соберет плагин
3. Запустится новая инстанция WebStorm с установленным плагином
4. Вы сможете тестировать плагин

## Текущий статус

✅ TypeScript код скомпилирован  
⚠️ Gradle wrapper не создан (нужен для запуска)  
⚠️ Gradle не найден в PATH (нужно установить или использовать IntelliJ IDEA)

## Рекомендация

Используйте **IntelliJ IDEA** для запуска - это самый простой способ, так как:
- IntelliJ IDEA имеет встроенную поддержку Gradle
- Автоматически создаст wrapper при необходимости
- Удобный интерфейс для отладки
