# 🚀 Ручной запуск плагина WebStorm

## Проблема: Java Runtime не найден

Если при запуске вы видите ошибку:
```
Unable to locate a Java Runtime
```

Это означает, что Java не установлена или не настроена правильно.

## Решение: Установить Java

### Вариант 1: Установить через Homebrew (рекомендуется)

```bash
# Установить OpenJDK 17 (требуется для плагина)
brew install openjdk@17

# Настроить переменные окружения
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
echo 'export JAVA_HOME="/opt/homebrew/opt/openjdk@17"' >> ~/.zshrc

# Применить изменения
source ~/.zshrc

# Проверить установку
java -version
```

### Вариант 2: Скачать с официального сайта

1. Перейдите на https://www.oracle.com/java/technologies/downloads/#java17
2. Скачайте JDK 17 для macOS
3. Установите пакет
4. Настройте JAVA_HOME:
   ```bash
   export JAVA_HOME=$(/usr/libexec/java_home -v 17)
   echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17)' >> ~/.zshrc
   ```

### Вариант 3: Использовать IntelliJ IDEA (самый простой)

Если у вас установлена IntelliJ IDEA, она уже содержит JDK:

1. Откройте проект в IntelliJ IDEA
2. IntelliJ IDEA автоматически использует свой JDK
3. Запустите через Gradle панель

## После установки Java: Запуск плагина

### Шаг 1: Проверьте Java

```bash
java -version
# Должно показать версию Java 17 или выше
```

### Шаг 2: Соберите TypeScript код

```bash
cd /Users/maksim.zhuikov/work/point-i18n
pnpm run build:webstorm
```

### Шаг 3: Запустите плагин

```bash
cd src/ide/webstorm

# Обычный запуск
./gradlew runIde

# Или с отладкой
./gradlew runIde --debug-jvm
```

## Пошаговая инструкция (полная)

### 1. Установка Java

```bash
# Проверьте, установлена ли Java
java -version

# Если нет - установите через Homebrew
brew install openjdk@17

# Настройте PATH
export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"
export JAVA_HOME="/opt/homebrew/opt/openjdk@17"

# Добавьте в ~/.zshrc для постоянного использования
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
echo 'export JAVA_HOME="/opt/homebrew/opt/openjdk@17"' >> ~/.zshrc
source ~/.zshrc
```

### 2. Проверка установки

```bash
java -version
# Должно показать: openjdk version "17.x.x"

echo $JAVA_HOME
# Должно показать путь к JDK 17
```

### 3. Сборка TypeScript

```bash
cd /Users/maksim.zhuikov/work/point-i18n
pnpm install  # если еще не установлено
pnpm run build:webstorm
```

### 4. Запуск плагина

```bash
cd src/ide/webstorm

# Первый запуск может занять несколько минут
./gradlew runIde
```

### 5. Что произойдет

1. Gradle загрузит зависимости (если еще не загружены)
2. Соберет плагин
3. Запустит новую инстанцию WebStorm
4. Откроется окно WebStorm с установленным плагином

## Альтернатива: Использовать IntelliJ IDEA

Если установка Java вызывает проблемы, используйте IntelliJ IDEA:

1. **Откройте проект:**
   - File > Open > выберите папку `point-i18n`

2. **Дождитесь синхронизации Gradle:**
   - IntelliJ IDEA автоматически использует свой JDK
   - Может занять несколько минут

3. **Запустите плагин:**
   - View > Tool Windows > Gradle
   - Найдите: `point-i18n > src/ide/webstorm > Tasks > intellij > runIde`
   - Двойной клик на `runIde`

## Решение проблем

### Проблема: "Unable to locate a Java Runtime"

**Решение:** Установите Java (см. выше)

### Проблема: "JAVA_HOME is not set"

**Решение:**
```bash
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
# Или
export JAVA_HOME="/opt/homebrew/opt/openjdk@17"
```

### Проблема: Gradle не может найти Java

**Решение:** Убедитесь, что JAVA_HOME установлен:
```bash
echo $JAVA_HOME
# Если пусто, установите (см. выше)
```

### Проблема: "Gradle build failed"

**Решение:**
```bash
cd src/ide/webstorm
./gradlew clean
./gradlew runIde
```

## Проверка готовности

Перед запуском убедитесь:

- [ ] Java установлена (`java -version` работает)
- [ ] JAVA_HOME настроен (`echo $JAVA_HOME` показывает путь)
- [ ] TypeScript код скомпилирован (`ls out/webstorm/extension.js`)
- [ ] Gradle wrapper существует (`ls src/ide/webstorm/gradlew`)

## Контакты

Если проблемы остаются, проверьте:
- Логи Gradle: `src/ide/webstorm/build/reports/`
- Документацию: `DEVELOPMENT.md`
- Структуру проекта: `ARCHITECTURE.md`

