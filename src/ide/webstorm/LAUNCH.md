# 🚀 Запуск плагина WebStorm

## Текущая ситуация

✅ **TypeScript код скомпилирован** в `out/webstorm/`  
✅ **Структура плагина создана**  
⚠️ **Gradle wrapper требует настройки** (проблема совместимости версий)

## Рекомендуемый способ: IntelliJ IDEA

**Самый простой и надежный способ:**

1. **Откройте проект в IntelliJ IDEA:**
   ```
   File > Open > выберите папку point-i18n
   ```

2. **Дождитесь синхронизации Gradle:**
   - IntelliJ IDEA автоматически определит Gradle проект
   - Может занять несколько минут при первом запуске
   - IntelliJ IDEA автоматически создаст правильный wrapper

3. **Запустите плагин:**
   - Откройте панель Gradle: `View > Tool Windows > Gradle`
   - Найдите: `point-i18n > src/ide/webstorm > Tasks > intellij > runIde`
   - **Двойной клик** на `runIde` для запуска
   - Или **правой кнопкой > Debug 'runIde'** для отладки

## Альтернативный способ: Установить Gradle 8.5

Если хотите запустить через командную строку:

```bash
# Установите Gradle 8.5 (не 9.x!)
# macOS:
brew uninstall gradle  # если установлена версия 9.x
brew install gradle@8

# Или скачайте с https://gradle.org/releases/
# Затем:
cd src/ide/webstorm
gradle wrapper --gradle-version 8.5
./gradlew runIde
```

## Что произойдет при запуске

1. TypeScript код будет автоматически скомпилирован
2. Gradle соберет плагин
3. Запустится новая инстанция WebStorm с установленным плагином
4. Вы сможете тестировать плагин

## Структура проекта

```
src/ide/webstorm/
├── src/main/java/          # Java код плагина
├── src/main/resources/     # Ресурсы (plugin.xml)
├── build.gradle.kts        # Конфигурация сборки
├── .run/                   # Run конфигурации для IntelliJ IDEA
└── out/webstorm/           # Скомпилированный TypeScript код
```

## Примечание

Текущая реализация использует TypeScript, который компилируется в JavaScript. 
Для полноценной работы плагина нужно создать Java/Kotlin bridge для интеграции с IntelliJ Platform API.

Подробнее см. `DEVELOPMENT.md`
